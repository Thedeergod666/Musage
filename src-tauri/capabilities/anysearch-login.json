{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "anysearch-login",
  "description": "anysearch-login window — permission for the login webview itself (window focus only). Window creation happens in Rust (src/anysearch_login.rs) and does not use webview permission. The webview loads an external URL and does not call createWebviewWindow.",
  "windows": ["anysearch-login"],
  "permissions": [
    "core:default",
    "core:window:allow-set-focus"
  ]
}
