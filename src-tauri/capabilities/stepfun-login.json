{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "stepfun-login",
  "description": "stepfun-login window — permission for the login webview itself (window focus only). Window creation happens in Rust (src/stepfun_login.rs) and does not use webview permission. The webview loads an external URL and does not call createWebviewWindow.",
  "windows": ["stepfun-login"],
  "permissions": [
    "core:default",
    "core:window:allow-set-focus"
  ]
}
