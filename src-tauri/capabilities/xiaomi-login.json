{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "xiaomi-login",
  "description": "xiaomi-login window — needs create-webview-window capability to spawn the SSO login webview (cookie extractor in src/xiaomi_login.rs). Only this window label gets the permission; floating + settings do not.",
  "windows": ["xiaomi-login"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-set-focus"
  ]
}
