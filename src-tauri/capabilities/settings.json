{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-extra",
  "description": "settings window — updater + process (for relaunch). process:default only applies to settings window, not floating/xiaomi-login.",
  "windows": ["settings"],
  "permissions": [
    "updater:default",
    "process:default"
  ]
}
